module ImgupSpec (spec) where

import Imgup

import Control.Exception (evaluate)
import System.Exit
import Test.Hspec

spec = undefined
-- spec :: Spec
-- spec =
--   describe "uploadAndReturnUrl" $ do
--     it "errors with no commandline args" $
--       evaluate uploadAndReturnUrl `shouldThrow` (errorCall "invalid args")
